import { SvelteComponentTyped } from 'svelte';
import type { ModalEvents, ModalProps } from './types';

export default class Modal extends SvelteComponentTyped<ModalProps, ModalEvents, { default: {} }> {}
